services:
  php82:
    image: chajr/php82-dev
    networks:
      - backend82
    command: ${RUN_COMMAND}
    volumes:
      - ${MOUNT_POINT}
    environment:
      - RABBIT_HOST=rabbitmq82
    depends_on:
      - rabbitmq82
  rabbitmq82:
    image: rabbitmq:3-management
    networks:
      - backend82
    ports:
      - "15672:15672"
      - "5672:5672"

networks:
  backend82:
    driver: bridge